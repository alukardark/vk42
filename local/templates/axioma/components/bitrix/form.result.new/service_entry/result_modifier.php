<?

if (!defined("B_PROLOG_INCLUDED") || B_PROLOG_INCLUDED !== true) die();

if ($arResult["isFormErrors"] != "Y" && $arResult["isFormNote"] == "Y")
{
    
}


foreach ($arResult["QUESTIONS"] as $SID => $arQuestion)
{
    if ($SID == "SERVICE_CATEGORY")
    {
        
    }
}


//printra($arResult);
?>